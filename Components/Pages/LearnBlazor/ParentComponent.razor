@page "/learnblazor/parentcomponent"

<h3>ParentComponent</h3>

<_ChildComponent Title="This title is passed as as parameter from ParentComponent" OnButtonClick="ShowMessage">
    Some text between the tags from ParentComponent
</_ChildComponent>

<p>@messageText</p>

<_ChildComponent Title="This title is passed as as parameter from ParentComponent">
    <button class="btn btn-primary">Click me</button>
</_ChildComponent>

<_ChildComponent Title="This title is passed as as parameter from ParentComponent">

</_ChildComponent>

@code {
    public string messageText = "";

    private void ShowMessage()
    {
        messageText = "Button inside child component was clicked";
    }
}
